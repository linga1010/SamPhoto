# Core dependencies for SAM Model application
torch>=2.0.0
torchvision>=0.15.0
numpy>=1.21.0
opencv-python>=4.5.0
Pillow>=8.0.0
gradio>=3.50.0
transformers>=4.21.0

# SAM (Segment Anything Model)
segment-anything>=1.0

# Additional ML/AI dependencies
accelerate>=0.20.0
tokenizers>=0.13.0

# Utility libraries (these are built-in Python modules, but listing for completeness)
# os - built-in
# io - built-in  
# tempfile - built-in

# Optional: If you want to use specific versions based on your environment
# torch==2.1.0+cu118 --index-url https://download.pytorch.org/whl/cu118
# torchvision==0.16.0+cu118 --index-url https://download.pytorch.org/whl/cu118